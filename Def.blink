option RemoteScope = "Struktura"
option ServerOutput = "src/Net/Server.luau"
option ClientOutput = "src/Net/Client.luau"

struct packet {
    Net: string,
    Event: string,
    Payload: buffer?
}

function RemoteFunction {
    Yield: Coroutine,
    Data: packet,
    Return: buffer?,
}

event ServerEvent {
    From: Client,
    Type: Reliable,
    Call: SingleSync,
    Data: packet?,
}

event ClientEvent {
    From: Server,
    Type: Reliable,
    Call: SingleSync,
    Data: packet?,
}